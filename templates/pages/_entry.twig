{% extends "_layout" %}

{% block main %}


                <h1>{{ entry.title }}</h1>
                <p>{{ entry.pageCopy }}</p>

                <span>Posted by <a href="">{{ entry.author }}</a> on {{ entry.postDate | date('M d, Y') }}</span>
            

{% endblock main %}